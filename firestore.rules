rules_version = "2";

service cloud.firestore {
  match /databases/{database}/documents {
    // The browser never talks directly to Firestore. All access goes through
    // the authenticated Node API using the Firebase Admin SDK.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
